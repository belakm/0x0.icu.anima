import React from 'react'
import TextWrapper from '../skeleton/TextWrapper'
import ScrollableContainer from '../skeleton/ScrollableContainer'

const LandingPage = () => (
  <TextWrapper>
    <ScrollableContainer lines={50}>
    Hello
    </ScrollableContainer>
  </TextWrapper>
)

export default LandingPage
